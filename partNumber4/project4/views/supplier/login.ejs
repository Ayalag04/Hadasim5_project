<!DOCTYPE html>
<html lang="he">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>כניסת ספק</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f0f8ff;
        color: #333;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        direction: rtl;
      }

      h1 {
        color: #35608b;
        text-align: center;
        margin-bottom: 20px;
      }

      form {
        background-color: #ffffff;
        padding: 20px 30px;
        border-radius: 12px;
        box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.1);
        width: 300px;
        direction: rtl;
        text-align: right;
      }

      label {
        font-size: 16px;
        display: block;
        margin-bottom: 5px;
        color: #555;
      }

      input[type="text"],
      input[type="tel"] {
        width: 100%;
        padding: 10px;
        margin-bottom: 15px;
        border: 1px solid #ccc;
        border-radius: 6px;
        font-size: 14px;
        background-color: #504c4c;
      }

      input:focus {
        border-color: #3f878f;
        outline: none;
        background-color: #504c4c;
      }

      button {
        background-color: #3f878f;
        color: white;
        border: none;
        padding: 12px;
        border-radius: 6px;
        width: 100%;
        font-size: 16px;
        cursor: pointer;
        transition: background-color 0.3s;
      }

      button:hover {
        background-color: #3f878f;
      }

      p {
        text-align: center;
        margin-top: 15px;
      }

      a button {
        margin-top: 5px;
        background-color: #3f878f;
      }

      a button:hover {
        background-color: #3f878f;
      }
    </style>
  </head>
  <body>
    <%- include('../partials/nav') %>
    <div>
      <h1>כניסת ספק</h1>
      <form action="/supplier/login" method="POST">
        <label for="supplier_name">שם ספק:</label>
        <input type="text" id="supplier_name" name="supplier_name" required />

        <label for="phone">מספר טלפון:</label>
        <input type="tel" id="phone" name="phone" required />

        <button type="submit">כניסת ספק</button>
      </form>

      <p>אין לך חשבון?</p>
      <a href="/supplier/signup"><button>הרשמה</button></a>
    </div>
  </body>
</html>
